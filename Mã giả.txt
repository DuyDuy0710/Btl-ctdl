function findPartitionIndex(nums):
    totalSum = sum(nums)
    leftSum = 0

    for i from 0 to length(nums) - 1:
        totalSum -= nums[i]
        if leftSum == totalSum:
            return i
        leftSum += nums[i]

    return -1
